<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>List of Shows</h1>
    <ul>
      <% @television_shows.each do |show| %>
      <li><%= "#{show.title} (#{show.network}) #{show.starting_year} #{show.genre} #{show.synopsis}"%></li>
      <% end %>
    </ul>
    <a href="television_shows/new">Add a New Show</a>
  </body>
</html>
